.idea
tmp
playground
main
/bin/*
!.vscode/launch.json
.vscode/*

node_modules
.env
dist
terraform/.terraform/*
terraform/.terraform.lock.hcl
# Terraform gitignore
# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
*.tfvars.json
!terraform/example.tfvars

# Ignore override files as they're usually used for local dev
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc
lambda.zip

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which might contain sensitive data
*.tfvars
*.tfvars.json

# Exclude override files as they're usually environment-specific
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Exclude CLI configuration files
.terraformrc
terraform.rc

# Exclude lock files if you're working in a team with different Terraform versions
# (though generally it's better to commit these)
# .terraform.lock.hcl

# Generated files
lambda.zip
*.zip
*.tar.gz

# Local environment files
.env
.env.*

# Sensitive variable files
secrets.tfvars
*secrets.tfvars

# Log files
*.log